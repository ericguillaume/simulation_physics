/**
 * Physics simulation configuration
 */

export const config = {
  // Photon emission settings
  photonEmission: {
    // If true, each electron can only emit one photon in its lifetime
    onePhotonPerElectron: true,
  },
};

